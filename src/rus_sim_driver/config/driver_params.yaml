/**:
  ros__parameters:
    # ── 驱动类型 ──
    # "sim"  — 仿真驱动（默认，配合 RobotSimDriver）
    # "real" — 真实机器人驱动（需设置 robot_ip）
    driver_type: "sim"

    # ── 机器人 IP（仅 real 模式需要） ──
    robot_ip: ""

    # ── 录制 / 回放 / 脚本文件路径 ──
    record_path:   ""
    playback_path: ""
    script_path:   ""
