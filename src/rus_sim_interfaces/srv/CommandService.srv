uint32 client_id   # 关联的前端 command id（bridge 透传；0=无），用于事件回关联 ack_id
string command
float64[] args
---
bool success
string message
float64[] result
# 文本结果（查询类指令：如回放的录音文件清单 / 当前文件名）；
# 数值结果放 result，文本放这里 —— bridge 原样透传到前端 reply.strings
string[] strings
